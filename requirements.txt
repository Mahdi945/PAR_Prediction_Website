# PAR Predictor — Web Application Dependencies (Streamlit Cloud installs this file)
#
# Deliberately unpinned. Streamlit Cloud builds on Python 3.14; webapp/
# requirements_web.txt holds the exact versions the local test suite was last
# run against, which may not all have 3.14 wheels. This file is what the
# deployed site actually resolves, so it tracks current releases.
#
# Editing this file also forces Streamlit Cloud to rebuild the environment and
# restart the Python process. That matters after a change that adds a new name
# to a core/ module: Cloud re-runs the page scripts on a push but keeps already
# imported modules in sys.modules, so new page code can meet an old core module
# and fail with ImportError until the process is restarted.

streamlit
pandas
numpy
pvlib
requests
plotly
joblib
scikit-learn
xgboost
pytz

# Exports on the Dataset Upload page (webapp/core/export.py).
# pyarrow is already a Streamlit dependency; openpyxl adds .xlsx.
openpyxl
pyarrow

# Live type-ahead in the place picker (webapp/core/places.py). Streamlit's own
# text_input and selectbox only rerun on Enter or blur, so neither can query
# the geocoder while the visitor is still typing.
streamlit-searchbox
