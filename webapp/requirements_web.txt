# PAR Predictor – Web Application Dependencies
# Install with: pip install -r requirements_web.txt
#
# ❗ EXACT VERSIONS — the combination the app and tests/ were last verified
# with (Python 3.14, 2026-09-19). Streamlit Cloud installs the unpinned root
# requirements.txt instead; these pins are for a reproducible local setup.

streamlit==1.60.0
pandas==3.0.2
numpy==2.4.4
pvlib==0.15.1
requests==2.32.3
plotly==6.9.0
joblib==1.5.3
scikit-learn==1.8.0
xgboost==3.2.0
pytz==2024.1

# Exports (core/export.py)
openpyxl==3.1.5      # Excel .xlsx
pyarrow==24.0.0      # Parquet — also a Streamlit dependency

# Live type-ahead in the place picker (core/places.py). Streamlit's own
# widgets cannot query while the user types; this component can.
streamlit-searchbox==0.1.24

# The IANA zone list behind the timezone dropdown. Present on most Linux
# images already; pinned here so the list is never empty.
tzdata==2026.1
